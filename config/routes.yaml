# API Routes will be defined in controllers using annotations
# This file can be used for global route configuration if needed

api:
    resource: ../src/Infrastructure/Controller/
    type: attribute
    prefix: /api

api_auth_login:
    path: /api/auth/login
